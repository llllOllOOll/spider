# Kamal deployment configuration for spider-hexagonal
service: spider-hexagonal

image: llllollooll/spider-hexagonal

servers:
  web:
    hosts:
      - 54.163.3.77

ssh:
  user: root
  keys:
    - ~/.ssh/aws-vps-key.pem

env:
  clear:
    HOST: "0.0.0.0"
    PORT: "8081"
    DATABASE_URL: "postgres://spider:spider@localhost:5432/spider"

proxy:
  ssl: false
  host: 54.163.3.77
  app_port: 8081
